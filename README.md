# Startup Autopsy: What Actually Kills Funded Indian Startups

**A data analytics project by Shreya Mishra** — B.Tech Computer Science, Chandigarh University
Machine Learning · Data Analytics · Business Analytics

---

## 1. Overview

Media coverage of Indian startup shutdowns is dominated by a single explanation: the **funding winter**. This project tests that assumption against a hand-compiled dataset of **52 real, named, funded Indian startups** that shut down between 2023 and 2025 — using each founder's own stated reason for closing — to find out whether "ran out of money" is really the whole story.

It turns out it isn't. Capital scarcity is the single largest cause (31%), but **Product-Market Fit failure and Regulatory Shock are statistically tied for second place at 19% each** — meaning nearly one in five documented, funded shutdowns wasn't a market failure at all, but a *policy* failure. The project goes one step further than description: it builds a small, honestly-validated **machine learning model** that predicts how long a startup will survive from its sector, funding level, and cause of death, beating a naive baseline by 34%.

This README explains **every file, every design decision, and every step of the pipeline** in detail, so the project can be understood, reproduced, or extended by anyone — including a version of you six months from now.

---

## 2. Project Files

| File | What it is | How it was built |
|---|---|---|
| `startup_shutdowns_india.csv` | The raw, hand-compiled dataset (52 rows x 8 columns) | Manually compiled by reading Inc42's 2023/2024/2025 startup shutdown reports and extracting structured fields for each startup |
| `startup_shutdowns_india_clean.csv` | The dataset after cleaning + the derived `lifespan_years` and `cause_category` columns | Generated automatically by `analysis.py` |
| `analysis.py` | Generates all 7 descriptive charts (figures 1-7) | Pandas for data wrangling, Matplotlib for charts |
| `model.py` | Trains, validates, and explains the predictive model (figures 8-9) | Scikit-learn (Linear Regression + Random Forest), Leave-One-Out Cross-Validation |
| `model_metrics.txt` | Plain-text summary of model performance numbers | Auto-generated by `model.py` |
| `runway_model.pkl` | The final trained Random Forest model, saved to disk | `joblib.dump()` in `model.py` -- reload with `joblib.load('runway_model.pkl')` |
| `build_notebook.py` | Assembles the full analysis + narrative into `Startup_Autopsy.ipynb` | Python script that programmatically builds the notebook's JSON structure, cell by cell, embedding the actual chart images so it opens pre-run |
| `Startup_Autopsy.ipynb` | The complete Jupyter notebook -- code, output, and narrative together | Output of `build_notebook.py` |
| `build_report.py` | Assembles the standalone HTML report | Python script that injects the charts (as base64 images) into a styled HTML template |
| `Startup_Autopsy_Report.html` | A polished, browser-readable report -- no Jupyter needed | Output of `build_report.py` |
| `build_docx.js` | Assembles the formal Word/PDF project report | Node.js script using the `docx` library |
| `Startup_Autopsy_Report.docx` | The full formal project report (title page, table of contents, numbered sections, figures, tables, references) -- 14 pages | Output of `build_docx.js` |
| `figures/` | All 9 chart PNGs, generated once and reused across the notebook, HTML report, and Word report | `analysis.py` (figures 1-7) and `model.py` (figures 8-9) |

**Why so many formats of the same analysis?** Each serves a different purpose: the **notebook** is what you push to GitHub to show your code; the **HTML report** is what you send someone a link to when they just want to read the findings; the **Word report** is what you submit as a formal academic or business deliverable, or attach to a job application.

---

## 3. Data Source & Collection Process

### 3.1 Where the data came from

All 52 startups are drawn from three annual feature articles published by **Inc42** -- India's largest startup-focused business media and intelligence platform:

- **2023**: "Plagued By Funding Winter & Bad Economics, Here Are 15 Indian Startups That Shut Down In 2023" -- https://inc42.com/features/plagued-by-funding-winter-bad-economics-here-are-15-indian-startups-that-shutdown-in-2023/ -- 15 startups
- **2024**: "2024's Startup Graveyard: 12 Indian Startups That Shut Down This Year" -- https://inc42.com/features/2024s-startup-graveyard-12-indian-startups-that-shut-down-this-year/ -- 12 startups
- **2025**: "From Burn To Breakdown: 25 Startups That Shut Down In 2025" -- https://inc42.com/features/25-indian-startups-shut-down-in-2025/ -- 25 startups

Each article profiles startups individually, citing the founders' own public statements (usually a LinkedIn post announcing closure, or a direct interview with Inc42) about why the company shut down. This matters methodologically: **the "cause of death" in this dataset is the self-reported reason, not an external forensic judgement** -- a limitation discussed further in Section 9.

### 3.2 How each row was built

For every startup mentioned in the three reports, the following fields were manually extracted and recorded:

| Column | Description | Example |
|---|---|---|
| `name` | Startup name | `Koo` |
| `sector` | Primary industry, assigned by reading the company description (19 distinct sectors recorded) | `Media & Content` |
| `founded_year` | Year the company was founded | `2020` |
| `shutdown_year` | Year the company shut down | `2024` |
| `funding_usd_mn` | Total disclosed lifetime funding in US$ millions, converted from INR at an approximate long-run rate where necessary. Left blank where no figure was disclosed (18 of 52 startups) | `50.0` |
| `primary_cause` | The specific, granular reason stated by the founder(s), kept in its original detailed form | `Funding Crunch / Failed Acquisition` |
| `city` | Headquarters city | `Bengaluru` |
| `source_year_report` | Which of the three Inc42 reports this entry came from | `2024` |

### 3.3 What this data is *not*

This is the single most important caveat in the whole project, so it's worth stating plainly: **this is not a census of startup failure in India.** Tracxn data, cited within the same Inc42 reports, shows that **over 28,000 Indian startups shut down in 2023-2024 alone.** This dataset covers only the ~52 that were prominent or well-funded enough to be individually named and profiled by a journalist. That means the sample is inherently biased toward:

- Better-funded companies (unfunded shutdowns are essentially invisible to media)
- Bengaluru-headquartered companies (India's startup media is concentrated there)
- Companies with a clean, quotable story (a founder willing to publicly explain the shutdown)

Every finding in this project should be read as **"among the funded, documented startups that make the news when they fail"** -- not as a statement about Indian startup failure in general.

---

## 4. Cleaning & Feature Engineering

This step is handled by `analysis.py` and mirrored in the notebook.

### 4.1 Computing lifespan

```python
df["lifespan_years"] = df["shutdown_year"] - df["founded_year"]
```

A simple derived field: how many years the company operated before shutting down.

### 4.2 Categorizing the cause of death

The raw `primary_cause` field is too granular to analyze directly -- it has **46 distinct values across 52 startups** (things like "Cash Burn / Flawed Biz Model" or "Regulatory (RBI UPI Ban)"). To find patterns, every entry was mapped into one of **7 broader cause categories** using a keyword-matching function, applied in a fixed priority order (so a startup with multiple contributing factors gets classified by its most structurally fundamental cause first):

```python
def categorize(cause: str) -> str:
    c = cause.lower()
    if 'regulatory' in c:
        return 'Regulatory Shock'
    if 'founder' in c or 'insolvency' in c or 'legal turmoil' in c or 'acquirer' in c:
        return 'Founder / Legal / Insolvency'
    if 'funding crunch' in c or 'no follow-on' in c or 'cash burn' in c:
        return 'Capital / Funding Crunch'
    if 'pmf' in c or 'traction' in c or 'retention' in c:
        return 'No Product-Market Fit'
    if 'business model' in c or 'unit economics' in c or 'biz model' in c or 'commoditised' in c or 'margins' in c:
        return 'Weak Business Model / Economics'
    if 'scal' in c or 'pivot' in c or 'demand' in c or 'debt' in c:
        return 'Scaling / Execution Failure'
    return 'Other'
```

**Why this priority order?** Regulatory shocks and founder/legal issues are checked first because they represent causes largely outside a founder's day-to-day control -- they are the most "external" and unambiguous categories. Capital, product-market fit, business model, and execution issues are progressively more "internal" to the company's own choices, and are checked in that order so a startup described with mixed language (e.g., "cash burn due to no follow-on funding") is bucketed consistently.

The resulting distribution:

| Cause Category | Count | % of Total |
|---|---|---|
| Capital / Funding Crunch | 16 | 31% |
| No Product-Market Fit | 10 | 19% |
| Regulatory Shock | 10 | 19% |
| Scaling / Execution Failure | 5 | 10% |
| Founder / Legal / Insolvency | 5 | 10% |
| Weak Business Model / Economics | 4 | 8% |
| Other | 2 | 4% |

---

## 5. Descriptive Analysis (`analysis.py`)

This script produces **7 charts** (Figures 1-7), each answering one specific question:

1. **`01_shutdowns_by_year.png`** -- How many documented shutdowns occurred each year? (15 / 12 / 25 for 2023/2024/2025 -- shaped by editorial coverage as much as the true rate, so read with caution)
2. **`02_cause_distribution.png`** -- The headline chart: what's the overall breakdown of causes across all 52 startups?
3. **`03_cause_by_year.png`** -- How has the *mix* of causes shifted year to year? (Reveals that Founder/Legal/Insolvency failures are a 2025-only phenomenon in this data)
4. **`04_lifespan_by_cause.png`** -- On average, how long did a startup survive before each type of death? (No-PMF failures die fastest at 3.3 years; Founder/Legal failures survive longest at 5.8 years)
5. **`05_funding_vs_lifespan.png`** -- Does more funding correlate with a longer lifespan? (Yes, moderately: r = 0.62 on log-funding)
6. **`06_funding_by_cause.png`** -- How much total capital was raised by startups in each cause category before they shut down?
7. **`07_sector_cause_heatmap.png`** -- Cross-tabulating sector against cause reveals sector-specific "signature" failure modes (e.g., Gaming & RMG dies almost exclusively from regulation)

All charts share a consistent visual design (color palette, fonts, gridlines) matching the rest of the portfolio, defined once at the top of `analysis.py` via `matplotlib.rcParams`.

---

## 6. Predictive Modelling (`model.py`)

### 6.1 The question

Beyond description, this project asks: **given only a startup's sector, funding level, and eventual cause of death, can a model predict how many years it survived?**

### 6.2 Why Leave-One-Out Cross-Validation (LOOCV)

Only 34 of the 52 startups have a disclosed funding figure, so the modelling dataset is just **34 rows**. With a sample this small, a single train/test split (e.g., 80/20) would be highly unstable -- whichever few rows happened to land in the test set could make the model look far better or far worse than it really is, purely by chance.

**LOOCV solves this**: the model is trained on 33 startups and tested on the 1 that was held out, and this is repeated 34 times so that *every single startup* gets used as the test case exactly once. Averaging the error across all 34 held-out predictions gives the most honest, stable estimate of real-world performance that this sample size can support.

### 6.3 Models compared

| Model | Why it was included |
|---|---|
| **Linear Regression** | Simple, interpretable baseline -- assumes a straight-line relationship between features and lifespan |
| **Random Forest Regressor** (300 trees, max depth 4) | Can capture non-linear patterns and feature interactions; the shallow max depth is deliberately chosen to avoid overfitting on only 34 rows |
| **Naive baseline** (always predict the mean lifespan, 4.18 years) | The honest floor -- any model that can't beat "just guess the average" isn't adding value |

### 6.4 Features used

- `log_funding` -- total funding raised, log-transformed (funding is heavily right-skewed: a few huge rounds like Builder.ai's $450M would otherwise dominate the model)
- `sector_grp` -- the startup's sector, with any sector appearing fewer than 2 times grouped into "Other" to avoid creating noisy one-startup categories
- `cause_category` -- the 7-category cause classification described in Section 4.2

### 6.5 Results

| Model | LOOCV MAE (years) | LOOCV R2 | Improvement vs. Baseline |
|---|---|---|---|
| Baseline (predict mean) | 1.90 | -- | -- |
| Linear Regression | 1.40 | 0.470 | +26% |
| **Random Forest** | **1.26** | 0.446 | **+34%** |

The Random Forest model was selected as the final model based on lowest MAE, saved to `runway_model.pkl`.

**Feature importance** (from the Random Forest, fit on the full 34-row dataset): `log_funding` alone accounts for **~73%** of the model's total predictive weight -- by far the dominant signal. Every sector and cause-of-death indicator contributes only a few percentage points individually. This quantitatively confirms the correlation finding from the descriptive analysis: funding level is the single strongest available predictor of survival time, but it explains less than three-quarters of the outcome -- meaning sector and cause-specific effects still matter.

### 6.6 Honest limitations of the model

This is deliberately framed as **illustrative, not deployable**:

- 34 labelled examples is too small a sample to trust for real forecasting decisions.
- An MAE of 1.26 years is a meaningful improvement over guessing, but still a large margin of error relative to typical lifespans of 2-10 years -- not precise enough for a single confident prediction about any one startup.
- The model's real value is demonstrating a **defensible workflow**: an appropriate validation method for small data (LOOCV), an honest baseline for comparison, and transparent feature importance -- rather than claiming a production-ready prediction tool.

### 6.7 How to reload and use the saved model

```python
import joblib
model = joblib.load("runway_model.pkl")

import pandas as pd
new_startup = pd.DataFrame([{
    "log_funding": 1.6,               # e.g. log1p($4M)
    "sector_grp": "Fintech",
    "cause_category": "Regulatory Shock",
}])
predicted_lifespan = model.predict(new_startup)
print(predicted_lifespan)
```

---

## 7. Key Findings

1. **Capital crunch is the single biggest documented cause of death (31%)** -- but Product-Market Fit failure and Regulatory Shock are statistically tied for second place (19% each). Regulation is a top-three killer of Indian startups, not a footnote.
2. **Regulatory risk is a constant background hazard, not a fading pandemic-era problem** -- it caused 4, 2, and 4 shutdowns in 2023, 2024, and 2025 respectively, with no sign of tapering off as the broader funding winter has thawed.
3. **Founder and governance failures are a distinctly 2025 phenomenon** in this dataset (0 -> 0 -> 5 across the three years), emerging only as the ecosystem's highest-profile, longest-running companies reach a scale where fraud and mismanagement become both material and publicly visible.
4. **Funding buys time, not survival** -- a correlation of r = 0.62 (log-funding vs. lifespan) is real but moderate; sector and specific cause of death explain the eventual outcome better than cheque size alone.
5. **Some sectors have a single dominant killer.** Gaming & RMG has essentially one cause of death (regulation, 4 of 4 documented cases); Enterprise SaaS and Healthtech share another (an inability to find a buyer for an otherwise viable product).
6. **A simple predictive model beats guessing by roughly one-third.** A Random Forest using only sector, funding level, and cause of death predicts lifespan with a Leave-One-Out cross-validated MAE of 1.26 years -- a 34% improvement over a naive baseline of 1.90 years.

---

## 8. Tools & Libraries Used

- **Python** -- core language for all data processing and analysis
- **Pandas** -- data loading, cleaning, grouping, cross-tabulation
- **NumPy** -- log transforms, correlation calculation
- **Matplotlib** -- all 9 charts, styled consistently with custom `rcParams`
- **Scikit-learn** -- `LinearRegression`, `RandomForestRegressor`, `LeaveOneOut`, `OneHotEncoder`, `ColumnTransformer`, `Pipeline`
- **Joblib** -- saving/loading the trained model
- **Jupyter** (notebook format) -- for the combined code + narrative deliverable
- **Node.js `docx` library** -- for generating the formal Word report

---

## 9. Limitations (Full List)

- **Sample size and selection bias**: n = 52 (34 for the model) is a curated media sample, not random or exhaustive. It skews toward better-funded, Bengaluru-headquartered, VC-backed companies.
- **Single stated cause per startup**: most shutdowns involve multiple contributing factors; assigning one dominant category per startup necessarily flattens some nuance.
- **Approximate funding figures**: amounts originally reported in INR were converted to USD at an approximate long-run rate; all figures are lifetime totals raised, not capital remaining at shutdown.
- **Self-reported causes**: the "cause of death" reflects what founders chose to say publicly, which may not always capture the full or most uncomfortable truth (e.g., a founder is more likely to cite "market conditions" than "we made a bad call").
- **The predictive model is illustrative, not deployable**: 34 rows is too small a sample to support real forecasting decisions.

---

## 10. Next Steps

- Extend the dataset with **Tracxn's aggregate shutdown counts** (15,921 in 2023; 12,717 in 2024) to weight these qualitative categories against the true, much larger base rate of failure.
- Continue tracking **2026 shutdowns** as they're reported, to see whether the Founder/Legal/Insolvency category keeps growing as a share of high-profile failures.
- Grow the funding-disclosed subsample (currently 34 startups) to make the predictive model meaningfully more reliable.
- Consider a follow-up project using **primary data** (e.g., a founder survey) to capture causes of failure that never make it into a public LinkedIn post.

---

## 11. How to Reproduce This Project

```bash
# 1. Install dependencies
pip install pandas numpy matplotlib scikit-learn joblib

# 2. Regenerate the descriptive charts (figures 1-7)
python analysis.py

# 3. Train and evaluate the predictive model (figures 8-9)
python model.py

# 4. (Optional) Rebuild the notebook and HTML report
python build_notebook.py
python build_report.py

# 5. (Optional) Rebuild the Word report -- requires Node.js and the `docx` npm package
node build_docx.js
```

---

*Prepared by Shreya Mishra -- B.Tech Computer Science, Chandigarh University -- September 2026*
